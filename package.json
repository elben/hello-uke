{
    "private": true,
    "scripts": {
      "build": "pulp --then 'cp assets/* dist/' build --to dist/app.js"
    },
    "devDependencies": {
      "pulp": "^12.3.",
      "purescript": "^0.12.2"
    }
  }
  